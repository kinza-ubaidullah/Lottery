import { DesktopChatSidebar } from "@/components/layout/DesktopChatSidebar";

export const ChatSidebar = () => {
    return (
        <div className="w-[320px] h-full flex-shrink-0 border-l border-white/[0.05]">
            <DesktopChatSidebar />
        </div>
    );
};
